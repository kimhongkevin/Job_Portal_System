spring:
  application:
    name: job_portal
  datasource:
    url: jdbc:postgresql://localhost:5432/jobportal_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: kimhongphb96@gmail.com
    password: frwtyxzxiooojlua
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: your-super-long-random-secret-key-min-32-characters-long
  expiration: 86400000

app:
  # Frontend page that completes the password reset flow (receives ?token=...)
  password-reset-url: http://localhost:3000/reset-password

file:
  uploads:
    upload-resume: uploads/resumes
    upload-image: uploads/images

springdoc:
  swagger-ui:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health,info
