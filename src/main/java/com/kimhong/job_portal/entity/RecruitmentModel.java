package com.kimhong.job_portal.entity;

public enum RecruitmentModel {
    // Visible to students, they can apply normally
    JOB_BOARD,
    // Hidden from public search, admin nominates candidates
    TALENT_POOL
}
